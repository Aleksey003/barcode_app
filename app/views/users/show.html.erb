<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<table class='table'>
	<%= render @user.barcodes %>
</table>

<%= link_to 'Edit', edit_user_path(@user), class: 'btn'%> |
<%= link_to 'Back', users_path, class: 'btn' %>|
<%= link_to 'New Barcode', new_barcode_path(user: @user) , class: 'btn' %>
